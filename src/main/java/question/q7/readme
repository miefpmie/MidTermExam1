Q7 本题由3个类和1个接口构成。其中人类(People)为父类，
现在需要你让美国人(AmericanPeople)和中国人(ChinesePeople)继承人类, 并实现Behaviour这个接口。
你需要做的事情：
1. 让美国人和中国人继承人类这个父类，并重写speakLanguage方法。
重写speakLanguage方法后，要求美国人返回的就是固定的"Hello"字符串, 中国人返回的就是固定的"你好"字符串。
2. 让美国人和中国人实现Behaviour接口，并实现接口中的方法，
要求对于吃饭(eat方法)，美国人吃饭吃的food是固定的字符串"牛排"(返回固定字符串"牛排"),中国人吃的是"鱼香肉丝"(返回固定字符串"鱼香肉丝")。
要求对于睡觉(sleep方法)，美国人睡觉时间(hours)为固定的8小时(返回固定的整数8)，中国人睡觉时间为7小时(返回固定的整数7)。